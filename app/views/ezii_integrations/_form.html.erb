<%= form_with(model: ezii_integration, local: true) do |form| %>
  <% if ezii_integration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ezii_integration.errors.count, "error") %> prohibited this ezii_integration from being saved:</h2>

      <ul>
        <% ezii_integration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
