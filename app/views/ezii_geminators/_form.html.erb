<%= form_with(model: ezii_geminator, local: true) do |form| %>
  <% if ezii_geminator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ezii_geminator.errors.count, "error") %> prohibited this ezii_geminator from being saved:</h2>

      <ul>
        <% ezii_geminator.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :graphql_query_text %>
    <%= form.text_area :graphql_query_text %>
  </div>

  <div class="field">
    <%= form.label :url_text %>
    <%= form.text_area :url_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
